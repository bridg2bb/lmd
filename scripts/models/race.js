export class race 
{
    constructor(name, minHealth, maxHealth, minStrength, maxStrength, minDex, maxDex) {
        this.name = name;
        this.minHealth = minHealth;
        this.maxHealth = maxHealth;
        this.minStr = minStrength;
        this.maxStr = maxStrength;
        this.minDex = minDex;
        this.maxDex = maxDex; 
    }
}